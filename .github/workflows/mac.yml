# This is a basic workflow that is manually triggered

name: Test on macOS

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'Person to greet'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "greet"
  greet:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Runs a single command using the runners shell
    - name: Setup SSH key
      env:
        SSH_KEY: ${{ secrets.MAC_CORP_SSH_PRIVATE_KEY }}
      run: echo "$SSH_KEY" > /tmp/sshkey && chmod 400 /tmp/sshkey
    - name: md5
      run: md5sum /tmp/sshkey
    - name: Test on macOS
      run: echo 'sudo -u alex sh -c "export PATH=/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin && export FORCE_COLOR=1 && brew upgrade earthly && earth -P github.com/earthly/earthly+test-all"' | ssh -i /tmp/sshkey -oStrictHostKeyChecking=no earthly@mac.corp.earthly.dev -p 8475

